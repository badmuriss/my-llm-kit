# Design

## Execution

Use one host-driver, read-only collector. Its capsule contains the discovery scope and returns URLs in the structured result summary. The collector cannot edit repository files.

The coordinator validates the result shape, runs the source-list check, opens every accepted source, and records claim-level evidence in `research/2026-08-20-visual-agent-orchestration-canvas.md`.

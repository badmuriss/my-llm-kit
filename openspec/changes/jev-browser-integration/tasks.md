# Jev integration

- [x] Inspect installed Jev/computer-use and repository installation boundaries.
- [x] Verify TypeSafe/OpenRouter contracts and compare Cua, compaction, Foreman.
- [x] Ship optional computer-use skill with reproducible OpenRouter runner; preserve installed user configuration and default install size.
- [x] Test routing, failure handling and a bounded real browser task with independent outcome checks.
- [x] Run relevant unittest modules, skill validation and ruff; record external experiment limits and recommendations.

Constraints: one writer; no real machine installer; preserve graph ownership, journal and deterministic checks. Experimental semantic supervision must not replace acceptance evidence. No live transcript rewrite.

Decision: ship browser integration and harness routing. Foreman remains experimental; compaction hook and Cua are evaluated but not installed. See research/2026-09-21-jev-harness.md for tests, limits and unresolved hypotheses. Existing machine integration is preserved.

## Follow-up: prioritize Codex in Orca

- [x] Clarify that delivered integration is skill routing and browser execution, not scheduler or compaction changes.
- [x] Inspect installed Codex protocol and Orca compaction adapter without modifying either.
- [x] Compare Foreman capabilities against Agent Graph at code boundaries.
- [x] Probe semantic supervision with synthetic observations and reproduce compaction's result-content blind spot.
- [x] Amend the existing research with concrete reuse recommendations and host limitations.

This follow-up is analysis. No compaction hook, automatic supervisor or host update is enabled. User priority: Codex inside Orca.

## Implement advisory evaluator (authorized follow-up)

- [x] Add bounded Jev/OpenRouter assessment of existing attempt observations, with typed probabilities and explicit advisory guidance.
- [x] Integrate a coordinator command and journal provenance; reject stale results, reuse unchanged observations, and preserve grades/worker lifecycle.
- [x] Add meaningful regressions for provider failure, malformed responses, stale state, replay, and no automatic intervention.
- [x] Exercise a real synthetic API request, run relevant unittest modules and ruff, and conduct an independent read-only contract review.
- [x] Document installed-skill invocation and limits; no compaction, second scheduler or automatic worker interruption.

Verification for evaluator: 58 CLI tests passed (241.387s), plus the newly added
pinned-command/no-key regression passed separately; 57 graph-core and 6 runtime-pin
tests passed. The terminal incompleteness regression was rerun after the review
fix and passed. Ruff, skill validation for agent-graph/impl, and diff whitespace
checks passed. A real synthetic OpenRouter request returned valid typed scores
without emitting advice below the evidence threshold. No actual Orca worker
end-to-end session or Windows run was observed. No new daemon or host integration
was installed; repository changes are uncommitted.

Independent read-only review: one material finding was fixed, terminal fallback
now honors Orca's outer `contentComplete: false` marker. Regression passed; reviewer
rechecked the final diff and found no remaining material issue in scope. Review
preserved generation/revision fences, advisory-only behavior, and existing cleanup.
